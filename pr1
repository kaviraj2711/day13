#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
    int a;
    scanf("%d",&a);
    for(int i=0;i<a;i++){
        for(int d=0;d<a;d++){
            printf("%d",i%2==0 ? 1:0);
        }
            printf("\n");
        
    }
    return 0;
}
